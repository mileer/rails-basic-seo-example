<% seo_tag('title', 'reports.show', {add_in: @report.title}) -%>
<% seo_tag('meta_desc', 'reports.show', {add_in: @report.title}) -%>

<h1><%= get_header_text(element: "h1", add_in: @report.title) -%></h1>

<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @report.title %>
</p>

<p>
  <strong>Author:</strong>
  <%= @report.author %>
</p>

<p>
  <strong>Published:</strong>
  <%= @report.published %>
</p>

<%= link_to 'Edit', edit_report_path(@report) %> |
<%= link_to 'Back', reports_path %>
